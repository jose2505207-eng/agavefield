# Base URL of the Agave Field FastAPI backend the frontend reads from.
NEXT_PUBLIC_API_BASE_URL=https://agavefield-nu.vercel.app

# Server-only API key for RBAC-protected endpoints (used via Next.js route
# handlers in a later phase — never exposed to the browser). Leave blank in dev.
API_KEY=
