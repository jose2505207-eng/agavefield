fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0
httpx==0.28.1
python-multipart==0.0.20
Pillow==11.1.0
pytest==8.3.4
boto3==1.35.92
# NOTE: the Streamlit dashboard's deps (streamlit, pandas, requests) live in
# dashboard/requirements.txt — kept out of here so the API stays slim enough
# for Vercel's serverless size limit. To run the dashboard locally:
#   pip install -r dashboard/requirements.txt
